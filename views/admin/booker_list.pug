extends layout

block content
  .container
    .row
      .col-sm-6.col-md-offset-1
        table(class='table table-striped table-condensed')
          thead
            tr
              th Экономист
              th Login
          tbody
            each booker in booker_list
              tr
                td #{booker.name}
                td #{booker.login}
