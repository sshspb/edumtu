extends layout

block content
            table(class='table table-striped table-condensed')
              thead
                tr
                  th Экономист
                  th login
              tbody
                each booker in booker_list
                  tr
                    td #{booker.name}
                    td #{booker.login}
